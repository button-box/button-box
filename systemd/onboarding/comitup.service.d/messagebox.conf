[Unit]
ConditionPathExists=/etc/messagebox-onboarding/enabled
Requires=avahi-daemon.service
Conflicts=messagebox.target messagebox-button.service messagebox-dash.service messagebox-nfc.service
Wants=NetworkManager.service
After=NetworkManager.service avahi-daemon.service
Before=messagebox.target messagebox-button.service messagebox-dash.service messagebox-nfc.service

[Service]
ExecStartPre=/usr/bin/test -s /etc/comitup.conf
